# Production screenshot pack

Captured from https://homex.selldone.shop/ on 2026-08-20 after the catalog fallback, responsive, and accessibility acceptance passes.

- Desktop: home, hero, categories, listing, product, blog, article, checkout, policy, account states, and Selldone credit bar
- Mobile: home, listing, and product at 390×844 CSS pixels
- `15-account-signedin.png` uses an intentionally simulated browser identity to exercise the signed-in layout. It is not proof of a real login and contains no live credential.
- Homex uses a lifestyle-photo hero without measured hotspots, so no hotspot capture is expected.
- The selected product's finish variants inherit the same base price; the variant screenshot proves image/selection state rather than a price delta.

